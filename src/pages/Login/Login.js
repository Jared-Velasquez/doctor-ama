import React from 'react';
import { Alert, Container } from 'react-bootstrap';

function Login (props) {
    return (
        <div>
            <Container>
            <Alert key="primary">
                Foo bar baz!!!!! This is Login page!!
            </Alert>
            </Container>
        </div>
    );
}

export default Login;